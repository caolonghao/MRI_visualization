const filenames = ['../files/eun_uchar_8.nii.gz'];
export const files = filenames.map(function(v) {
    return v;
});


export const colors = {
  red: 0xff0000,
  blue: 0x0000ff,
  darkGrey: 0x353535,
};
